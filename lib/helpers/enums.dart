enum DeviceType { mobile, tablet, web, mac, windows, linux, fuchsia }

enum Header { http, auth }

enum Source { network, asset }

enum TwinAnim { bottom_to_top, right_to_left }

enum Status { upcoming, completed, cancelled, rescheduled }
